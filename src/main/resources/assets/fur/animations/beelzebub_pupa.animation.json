{
	"format_version": "1.8.0",
	"animations": {
		"cocoon.model.idle": {
			"loop": true,
			"animation_length": 2,
			"bones": {
				"base": {
					"rotation": {
						"vector": [0, "-math.cos(query.anim_time * 180) * 5", 0]
					}
				},
				"seg0": {
					"rotation": {
						"vector": [0, "-math.cos(query.anim_time * 180-90) * 5", "math.cos(query.anim_time * 180-90) * 3"]
					},
					"scale": {
						"vector": ["1 + math.sin(query.anim_time * 360-90) * 0.05", "1 + math.sin(query.anim_time * 360-180) * 0.05", 1]
					}
				},
				"seg1": {
					"rotation": {
						"vector": [0, "-math.cos(query.anim_time * 180) * -10", 0]
					}
				},
				"seg2": {
					"rotation": {
						"vector": [0, 0, "math.cos(query.anim_time * 180) * 3"]
					},
					"scale": {
						"vector": ["1 + math.sin(query.anim_time * 360) * 0.05", "1 + math.sin(query.anim_time * 360-90) * 0.05", 1]
					}
				},
				"seg3": {
					"rotation": {
						"vector": [0, "-math.cos(query.anim_time * 180+90) * -5", "math.cos(query.anim_time * 180+90) * 3"]
					},
					"scale": {
						"vector": ["1 + math.sin(query.anim_time * 360+90) * 0.05", "1 + math.sin(query.anim_time * 360+180) * 0.05", 1]
					}
				}
			}
		}
	}
}